const a = 42;
const b = 96;

const templateString = `Сумма ${a} и ${b} равна ${a + b}`;

console.log(templateString);

const multilines = `\
Этот текст
разбит
на несколько
строк
`;
console.log(multilines);
